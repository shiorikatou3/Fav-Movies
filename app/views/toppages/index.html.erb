<%= render "layouts/jumbotron" %>

<% if logged_in? %>
  <div class="container">
    <h3 class="text-center mt-4 mb-5">タイムライン</h3>
  </div>
  
  <%= render "reviews/review", reviews: @reviews %>
  
  
<% else %>
  <div class="container">
    <h3 class="text-center mt-4 mb-5">人気のレビュー</h3>
  </div>

　<%= render "reviews/review", reviews: @reviews %>

<% end %>

<%= link_to 'View users', users_path %>
