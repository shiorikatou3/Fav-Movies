<%= render "user.show", user: @user %>
  
<div class="mb-5">  
  <%= render "usersbar" %>
</div>

<%= render 'users', users: @followers %>

<div class="pagination-sm mt-5 mb-5">
 <%= paginate @followers %>
</div>